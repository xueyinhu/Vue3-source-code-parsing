import type {Component} from './types/component'

function Vue(options?: Record<string, any>) {
}


Vue.prototype._init = function(options?: Record<string, any>) {
    const vm: Component = this
}


export default Vue